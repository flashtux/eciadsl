# the following lines are generated by the configure script 
# DO NOT EDIT THIS FILE BUT THOSE INSTEAD: 
# Makefile.config.sk 

CFLAGS += -ansi
LDFLAGS += -lpthread

INSTALL_EXEC = install -c -p -m 755
INSTALL_DATA = install -c -p -m 644
INSTALL_DIR = install -c -p -d -m 755

STRIP = strip
PRODUCT_ID = ECIADSL
PRODUCT_NAME = ECIADSL USB modem driver
PRODUCT_VERSION ?= 0.8
PREFIX = /usr/local/
ETC_PREFIX = /
CONF_PREFIX = /
DOC_PREFIX = /usr/local/
BIN_DIR = $(PREFIX)bin
ETC_DIR = $(ETC_PREFIX)etc
CONF_DIR = $(CONF_PREFIX)etc/eciadsl
DOC_DIR = $(DOC_PREFIX)doc/eciadsl
CFLAGS += -pedantic -Wall -W
